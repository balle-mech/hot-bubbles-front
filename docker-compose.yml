services:
  dev:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development

  build:
    build: .
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run build
